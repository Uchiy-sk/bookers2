<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/info', user: @user %>
      <%= render 'books/new', book: @book %>
    </div>
    <div class="col-md-8 offset-md-1">
      <!-- 一覧表示 -->
      <h2>followers</h2>
      <%= render 'follow_list', users: @followers %>
    </div>
  </div>
</div>
